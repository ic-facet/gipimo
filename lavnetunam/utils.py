"""
Módulo `utils` eliminado / deshabilitado.

Se dejó este archivo como marcador para evitar errores si algún import
accidental lo referencia. Las funciones fueron removidas a petición del
usuario. No uses este módulo; los endpoints ahora están configurados como
stubs en `core.views` y la plantilla `graficas.html` dibuja gráficos de
ejemplo directamente en el cliente usando Chart.js.
"""
